#链表最终汇总知识

# <mark>203_移除节点元素==val的节点------移除元素用虚拟节点---且移除B必须知道A和C</mark>

1- True None False 这些首字母必须大写

2-明确便利条件current，什么时候用next什么时候用current

这里判别： <mark>利用虚拟节点的时候，一般都是current.next（由于虚拟节点不用判断）其次一定要更新遍历条件！！！最后就是如果有if else一定看遍历条件是否放在else里面</mark>

3-最后返回值的确定：有虚拟返回虚拟，无虚拟返回原链表

<mark>由于定义链表时候市以头节点位置来确定的，所以一定要明确当前位置</mark>。



# 19-_删除倒数第n个节点

#思路1--先获取链表全部长度，然后找到对应的三个位置直接删除(采用虚拟头节点）

<mark>类函数互相调用时候要加self.函数</mark>

1-<mark>类属性在类函数里面反复累加，会改变值！！！对象属性同理！！！</mark>

2-遍历的步伐容易错

长度为6,走6步走到none也对应while和for的前闭后开

加入虚拟节点则为走7步到末尾

<mark>while 和 for while 3 遍历三次</mark>

<mark>所以删除倒数第2个元素则对应走4步（加虚拟） 走3步（不加虚拟）</mark>



#思路2--直接双指针法：快指针和慢指针之前存在一定的位置关系  
<mark>快指针先走n+1步，慢指针再走。这样当fast走到null，慢指针刚好走到目标前一个位置</mark>  


<mark>--加虚拟节点</mark>#<mark>要求：可以根据这样的一句话提迅速写出代码</mark>

<mark>双指针依旧current，删除元素依旧虚拟节点</mark>







# 24交换相邻节点--虚拟节点加标准步骤A--B--C

#这里避免none.next 以及none.val出现  
#采用虚拟节点 虚拟-B,在B--A,最后A--C

1-#不判断虚拟节点，故为current.next但是由于又要指向第三个存在

while current.next and current.next.next





# 206反转链表---标准模板直接背4步--不加虚拟节点

# 142 环形链表---双指针法--不加虚拟节点

#步骤：fast每次走两步，slow每次走一步 当fast = slow的时候，找到环形  

找到环起点，slow=head 然后fast不变继续走，都是一步当相遇时候slow即为起始点

#开始写



# 0207面试题  找到单链表相交节点的起始位置

获取两链表的长度然后让他们的末尾位置对齐，接下来从短的一方作为起始位置找相等节点
